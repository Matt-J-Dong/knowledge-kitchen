A simple website following a simple continuous deployment workflow.

The `dist` directory contains the raw files used to generate the website. The `.github/workflows` directory contains the deploy script using GitHub Actions.
